spring:
  application:
    name: StudyFlow
  datasource:
    url: jdbc:postgresql://localhost:5432/studyflow
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
